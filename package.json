{
  "name": "document-reliability-assurance",
  "version": "0.1.0",
  "description": "DRA (Document Reliability Assurance) — a deterministic, evidence-auditable reference evaluator for assessing claim support in documents. Publication edition DRA-GC-1 (evaluator 0.1.2, pipeline 1.0, model/schema 0.1.0, corpus DRA-CORPUS-1.0.0).",
  "private": true,
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^25.9.4",
    "typescript": "~5.9.3",
    "vitest": "^4.1.10"
  }
}
